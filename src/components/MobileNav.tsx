import { LayoutDashboard, MoreHorizontal, PieChart, Plus, ReceiptText } from 'lucide-react'
import type { PageKey } from '../hooks/useHashPage'
export function MobileNav({ page, setPage, onAdd, onMore }: { page: PageKey; setPage:(p:PageKey)=>void; onAdd:()=>void; onMore:()=>void }){
  const item=(key:PageKey,Icon:typeof LayoutDashboard,label:string)=><button onClick={()=>setPage(key)} className={`mobile-tab ${page===key?'text-teal-700 dark:text-teal-300':''}`}><Icon size={20}/><span>{label}</span></button>
  return <nav className="fixed inset-x-0 bottom-0 z-40 grid grid-cols-5 border-t border-slate-200 bg-white/95 px-2 pb-[max(8px,env(safe-area-inset-bottom))] pt-2 backdrop-blur-xl dark:border-slate-800 dark:bg-slate-950/95 lg:hidden">{item('dashboard',LayoutDashboard,'Home')}{item('transactions',ReceiptText,'Transactions')}<button onClick={onAdd} className="mobile-tab -mt-6 text-teal-700 dark:text-teal-300"><span className="grid size-12 place-items-center rounded-2xl bg-gradient-to-br from-teal-600 to-indigo-600 text-white shadow-lg shadow-teal-600/25"><Plus size={24}/></span><span>Add</span></button>{item('budget',PieChart,'Budget')}<button onClick={onMore} className={`mobile-tab ${['reports','calendar','settings','ai','billing','founder'].includes(page)?'text-teal-700 dark:text-teal-300':''}`}><MoreHorizontal size={20}/><span>More</span></button></nav>
}
